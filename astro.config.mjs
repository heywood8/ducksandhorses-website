import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://heywood8.github.io/ducksandhorses_website',
  integrations: [],
  build: {
    format: 'directory'
  }
});
