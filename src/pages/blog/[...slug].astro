---
// Legacy catch-all blog route placeholder.
// Returning an empty array prevents static generation and avoids build errors.
export async function getStaticPaths() {
	return [];
}
---
<!-- Legacy catch-all blog route intentionally disabled. Safe to delete this file. -->