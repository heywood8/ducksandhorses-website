---
import Layout from '../../layouts/Layout.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<Layout title="Schedule" description="Book a first meeting or a regular therapy session." canonical={base + '/schedule/'}>
  <h1>Schedule</h1>
  <p style="max-width:60ch;">Use the booking widgets below to reserve time. If you have trouble loading them, use the direct links provided. Not for emergencies—if you are in crisis, visit the <a href={base + '/emergency'}>Emergency & Crisis Resources</a> page instead.</p>
  <section aria-labelledby="first-meeting-heading" style="margin-top:var(--space-8);">
    <h2 id="first-meeting-heading">Book a First Meeting</h2>
    <p class="muted" style="font-size:var(--font-size-sm);margin-top:0;">30 minute introductory session.</p>
    <noscript><p>JavaScript is required to display the booking widget. Book directly at <a href="https://calendly.com/masachusec1/30min" rel="nofollow noopener">Calendly 30min</a>.</p></noscript>
    <div class="calendly-inline-widget" data-url="https://calendly.com/masachusec1/30min" style="min-width:320px;height:700px;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface-subtle);"></div>
  </section>
  <section aria-labelledby="regular-session-heading" style="margin-top:var(--space-9);">
    <h2 id="regular-session-heading">Book a Regular Session</h2>
    <p class="muted" style="font-size:var(--font-size-sm);margin-top:0;">Standard follow-up session.</p>
    <noscript><p>JavaScript is required to display the booking widget. Book directly at <a href="https://calendly.com/masachusec1/clone" rel="nofollow noopener">Calendly regular session</a>.</p></noscript>
    <div class="calendly-inline-widget" data-url="https://calendly.com/masachusec1/clone" style="min-width:320px;height:700px;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface-subtle);"></div>
  </section>
  <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
</Layout>
